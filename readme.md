NPM Filtering on large datasets.

Implemented paging and filtering on server.

To get started:
    * make sure mongodb is started! 
    * run: npm seed (this will set you up with some data)
    * run: npm start